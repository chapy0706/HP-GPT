{
  "name": "static-site-guardrails-offline",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "node scripts/static-server.mjs --port 4173",
    "verify:offline": "node scripts/offline-verify.mjs",
    "test:offline": "node --test tests/offline-contract.test.mjs",
    "verify": "npm run -s verify:offline && npm run -s test:offline"
  }
}
